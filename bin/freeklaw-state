#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
FREEKLAW_HOME=$(dirname "$SCRIPT_DIR")

exec "$FREEKLAW_HOME/runtime/python/bin/python" \
    "$FREEKLAW_HOME/lib/freeklaw_state.py" "$@"
